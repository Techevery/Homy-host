// import 'dotenv/config';  // Loads your .env file (install with npm install dotenv if needed)
// import { defineConfig, env } from 'prisma/config';

// export default defineConfig({
//   schema: './prisma/schema.prisma',  // Relative path to your schema
//   migrations: {
//     path: './prisma/migrations',  // Relative path to migrations folder
//   },
//   datasource: {
//     url: env('DATABASE_URL')
//   }
// });